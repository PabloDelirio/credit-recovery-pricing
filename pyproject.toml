[build-system]
requires = ["setuptools>=61.0"]
build-backend = "setuptools.build_meta"

[project]
name = "credit_pricing"
version = "0.1.0"
description = "Otimização de desconto e parcelamento em recuperação de crédito"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["src"]